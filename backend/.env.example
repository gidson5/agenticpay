NODE_ENV=development
PORT=3001
CORS_ALLOWED_ORIGINS=http://localhost:3000
JOBS_ENABLED=true
QUEUE_ENABLED=true
STELLAR_NETWORK=testnet
RATE_LIMIT_FREE=100
RATE_LIMIT_PRO=300
RATE_LIMIT_ENTERPRISE=1000
RATE_LIMIT_WINDOW_MS=900000
COMPRESSION_THRESHOLD=1024

# Relayer Configuration (off-chain authorization / gasless transactions)
# RELAYER_SECRET_KEY=S... (Stellar secret key for the relayer account)
# RELAYER_PUBLIC_KEY=G... (Stellar public key for the relayer account)

# IP Allowlist (comma-separated CIDR ranges)
# IP_ALLOWLIST=10.0.0.0/8,192.168.1.0/24
# IP_ALLOWLIST_ENABLED=false
# IP_ALLOWLIST_BYPASS_ENABLED=false
# IP_ALLOWLIST_BYPASS_EXPIRY_MS=1800000

# Stripe card payments
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
